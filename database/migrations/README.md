# Migrations

The executable migration history is maintained in `backend/prisma/migrations/`.
This directory is reserved for database-level assets that are not owned by Prisma;
do not duplicate migration history here.
