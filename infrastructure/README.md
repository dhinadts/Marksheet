# Infrastructure

Local containers live under `docker/`. AWS and Terraform implementation will be added during the production deployment phase; empty production infrastructure is not presented as complete in Phase 1.

